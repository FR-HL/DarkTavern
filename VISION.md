# DarkTavern 愿景与最初目标

> 本文是项目的**北极星**。
> 做着做着很容易跑偏——我们已经跑偏过一次：从"自动购买装备"一路做成了"查价 + UI 重构"。
> 以后任何新功能、任何重构，动手前先回看本文，问一句：**"这离最初目标更近了吗？"**
> 若不能，先停，别做。

---

## 一、最初目标（一句话）

做一个 **Dark and Darker 的自动购买装备工具**。

不是查价器，不是 wiki，不是配装站——是**能自己出手买**的工具。查价、UI 都是为这个目标服务的手段，不是终点。

---

## 二、最初确认的需求（自动购买）

- **场景**：游戏内市场
- **交互方式**：图像识别 + 模拟点击（截图 → 识别列表 → 点击购买 → 点确认）
- **触发条件**：指定物品精确匹配（物品名 + 价格 + 属性词条）
- **自动化程度**：按物品可配置——信任的走全自动，其余走半自动（弹确认）
- **安全护栏**：每日 / 每次预算上限、购买前确认、购买日志记录
- **运行环境**：本地运行、中文客户端、1080p
- **反作弊**：游戏带 EAC，需考虑规避——模拟输入走硬件级方案（如 Interception 驱动）+ 鼠标轨迹/延迟随机化

---

## 三、延伸愿景（放长远）

- 接入 **AI / 智能体**，帮主人"管理和操作"：自动捡漏、自动购买、背包整理……
- 心智模型：**智能体 = 会做决定的脑子（LLM）+ 一组带名字的命令（工具）+ 桌面端当它的手和眼**。
- 命令一次封装，**人和 AI 都能调同一组**——这是为智能体铺的路，不是额外开销。
- 数据可与 dndwiki 共用（翻译表 / 物品库 / 价格），但**代码各管各**：能看屏幕的归桌面端，不用看屏幕的归网站，靠接口统一，不靠合并仓库。

---

## 四、已确立的原则 / 护栏（防跑偏）

- **后端纯 Python**（已落地 FastAPI），壳用 Electron，**单窗口**。
- **视觉**：类苹果 / 微软的系统纪律 + 暗酒馆魂的融合。
- **只做真有功能的**，不做占位假卡——"那些归网站"。
- **只读屏幕**：不注入 DLL、不读游戏内存。行为清白是真护身符。
- **白名单拿不到、也不该追**；自动操作（模拟点击）那一档连白名单也救不了，靠"像人 + 不注入" + 接受封号风险。
- **一格一格演进**：停在任意一格都必须是完整能用的软件，不预先搭空架子。

---

## 五、距离最初目标还差什么（现状对照）

**已具备**
- 眼：截图（mss）+ 提示框检测（YOLO DNN）+ 中文 OCR（RapidOCR）+ 中→英翻译
- 查价：DarkerDB API
- 脸：主页 + 设置 + 词条 + 游戏内悬浮窗
- 命令雏形：FastAPI 路由（health / window / scan / translate / mapping）

**还缺（自动购买 & 智能体的卡点）**
- **手**：模拟点击 / 键盘 / 拖格子 —— 自动购买与背包整理都卡在这
- **命令字典**：把现有能力登记成"名字 + 说明 + 参数"的表，AI 可直接当 tools 读
- **智能体循环**：LLM 决策 + 调命令的执行框架
- **市场 UI 识别**：市场列表 ≠ 物品 tooltip，可能要模板匹配 / 逐行 OCR / 逐格悬停

---

## 六、决策检查清单（动手前过一遍）

- [ ] 这改动让"自动出手买 / 智能体操作"更近了吗？
- [ ] 还是又在打磨"只读"那一层（查价 / UI）？若是，值不值得现在做？
- [ ] 有没有引入注入 / 读内存 / 提前搭空架子？
- [ ] 做完这一格，软件是否仍完整能跑？

**任何一条答不上来，先停，回看第一节。**
